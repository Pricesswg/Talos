{
  "config": {
    "step": {
      "user": {
        "title": "Talos",
        "description": "Talos maps where your data comes from and where it goes.\n\nA DNS resolver is optional: AdGuard Home or Pi-hole v6. Without one, Talos still tells you what stops working when the internet drops, using what Home Assistant declares about itself. It cannot tell you which devices talk to the outside world, and the report says so rather than leaving it blank.\n\nFor full coverage the resolver should also know who is who: AdGuard Home by serving DHCP, Pi-hole through the network table it keeps from what it sees on the wire. A router's own DNS keeps no query log Talos can read; point the router at one of these instead.",
        "data": {
          "resolver_kind": "Resolver",
          "adguard_url": "Resolver address",
          "adguard_username": "Username (AdGuard Home only)",
          "adguard_password": "Password",
          "verify_ssl": "Verify the SSL certificate",
          "mqtt_host": "Broker address (optional)",
          "mqtt_port": "Broker port",
          "mqtt_username": "Broker username",
          "mqtt_password": "Broker password",
          "mqtt_tls": "Broker uses TLS"
        },
        "data_description": {
          "adguard_url": "Where the resolver answers. AdGuard Home: for example http://192.168.1.10:3000, or the add-on's internal hostname http://a0d7b954-adguard:3000, which survives an IP change. Pi-hole v6: the address of its web interface, for example http://192.168.1.11, no path. Do not use 127.0.0.1 unless Home Assistant runs on host networking: inside a container that address is the container itself. Leave empty to run without observed data.",
          "adguard_username": "AdGuard Home only. Leave empty if it has no authentication. Pi-hole has no username: give its web password below, or an app password."
        }
      },
      "reconfigure": {
        "title": "Talos: DNS resolver",
        "description": "Change the resolver, its address or its credentials. Leave the address empty to go back to a declared-only report, without the observed side.",
        "data": {
          "resolver_kind": "Resolver",
          "adguard_url": "Resolver address",
          "adguard_username": "Username (AdGuard Home only)",
          "adguard_password": "Password",
          "verify_ssl": "Verify the SSL certificate",
          "mqtt_host": "Broker address (optional)",
          "mqtt_port": "Broker port",
          "mqtt_username": "Broker username",
          "mqtt_password": "Broker password",
          "mqtt_tls": "Broker uses TLS"
        },
        "data_description": {
          "adguard_url": "Where the resolver answers. AdGuard Home: for example http://192.168.1.10:3000, or the add-on's internal hostname http://a0d7b954-adguard:3000, which survives an IP change. Pi-hole v6: the address of its web interface, for example http://192.168.1.11, no path. Do not use 127.0.0.1 unless Home Assistant runs on host networking: inside a container that address is the container itself. Leave empty to run without observed data."
        }
      },
      "discovered": {
        "title": "AdGuard Home found",
        "description": "Talos found an AdGuard Home instance answering at {url} and filled the form with it. Check the values and confirm, or change them. Clear the address to run without observed data.",
        "data": {
          "resolver_kind": "Resolver",
          "adguard_url": "Resolver address",
          "adguard_username": "Username (AdGuard Home only)",
          "adguard_password": "Password",
          "verify_ssl": "Verify the SSL certificate",
          "mqtt_host": "Broker address (optional)",
          "mqtt_port": "Broker port",
          "mqtt_username": "Broker username",
          "mqtt_password": "Broker password",
          "mqtt_tls": "Broker uses TLS"
        },
        "data_description": {
          "adguard_url": "Where the resolver answers. AdGuard Home: for example http://192.168.1.10:3000, or the add-on's internal hostname http://a0d7b954-adguard:3000, which survives an IP change. Pi-hole v6: the address of its web interface, for example http://192.168.1.11, no path. Do not use 127.0.0.1 unless Home Assistant runs on host networking: inside a container that address is the container itself. Leave empty to run without observed data.",
          "adguard_username": "AdGuard Home only. Leave empty if it has no authentication. Pi-hole has no username: give its web password below, or an app password."
        }
      }
    },
    "error": {
      "cannot_connect": "AdGuard Home did not answer at that address.",
      "invalid_auth": "AdGuard Home rejected those credentials.",
      "unknown": "Unexpected error. Check the logs.",
      "mqtt_invalid_auth": "The broker rejected those credentials.",
      "mqtt_cannot_connect": "Could not reach the broker at that address."
    },
    "abort": {
      "already_configured": "Talos is already set up. One instance maps the whole house.",
      "reconfigure_successful": "Resolver connection updated. Talos will use it on the next scan."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Talos settings",
        "description": "Retention is not optional: the query log grows one row per client-and-name pair, so both limits below are enforced on every scan. The row cap is what actually bounds the database file.",
        "data": {
          "scan_interval_minutes": "Scan interval",
          "observation_days": "Forget observations after",
          "max_observations": "Maximum stored observations",
          "scan_history": "Scan snapshots kept",
          "page_size": "Query log page size",
          "max_pages": "Maximum pages per scan",
          "domain_rules_path": "Extra domain rules file",
          "zone_trusted_lan": "Trusted LAN subnet",
          "zone_iot_vlan": "IoT VLAN subnet",
          "zone_guest": "Guest network subnet",
          "check_rules_path": "Extra posture checks file",
          "retention_days": "Keep data for (days)",
          "auto_retention": "Auto sizing"
        },
        "data_description": {
          "observation_days": "A device replaced months ago should stop shaping today's report.",
          "max_observations": "The limit that bounds the file. Oldest observations are dropped first.",
          "scan_history": "Snapshots are a convenience; the observations are the historical record.",
          "max_pages": "A budget, so one scan cannot walk a busy log for minutes.",
          "domain_rules_path": "Absolute path to a JSON or YAML file. Your rules are layered on top of the built-in list, never replacing it.",
          "zone_trusted_lan": "For example 192.168.1.0/24. Until you set it, the network-zone checks report themselves as not runnable rather than passing.",
          "zone_iot_vlan": "Several ranges, comma separated. An address matching none of them stays 'unknown': it is never assumed to be trusted.",
          "check_rules_path": "Absolute path to a JSON or YAML file replacing the check list. Severity and remediation are data, not code.",
          "retention_days": "The one number that matters. With auto sizing on, the three limits below are derived from it and from the install's measured rate.",
          "auto_retention": "On: observation age, row ceiling and scan documents kept are derived. Off: the three limits below are yours."
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "local_with_egress": {
        "name": "Local devices phoning home"
      },
      "entities_offline": {
        "name": "Entities that stop offline"
      },
      "autonomy": {
        "name": "Offline autonomy"
      },
      "devices_talking_out": {
        "name": "Devices talking outside"
      },
      "unverified_checks": {
        "name": "Unverified checks"
      },
      "correlation": {
        "name": "Correlation coverage"
      },
      "database_size": {
        "name": "Database size"
      },
      "last_scan": {
        "name": "Last scan"
      },
      "findings_high": {
        "name": "High severity findings"
      }
    },
    "binary_sensor": {
      "blind_spot": {
        "name": "Blind spot"
      }
    }
  },
  "services": {
    "export_report": {
      "name": "Export the report",
      "description": "Writes the self-contained report of the last scan. The default target is config/talos/, deliberately not config/www/: that directory is served at /local/ without authentication and this report lists addresses, MACs and network topology.",
      "fields": {
        "path": {
          "name": "Path",
          "description": "Relative to the configuration directory, or absolute inside it. Leave empty for the default."
        },
        "format": {
          "name": "Format",
          "description": "html for a readable, archivable file; json for the raw scan and derivations."
        }
      }
    },
    "refresh": {
      "name": "Run the scan again",
      "description": "Forces a fresh collection without waiting for the interval."
    }
  },
  "selector": {
    "resolver_kind": {
      "options": {
        "adguard": "AdGuard Home",
        "pihole": "Pi-hole v6"
      }
    }
  }
}
